import React from 'react';

function Footer() {
    const footerStyles = {
        backgroundColor : 'black',
        color : 'white',
    }
    return (
        <footer style={footerStyles}>Let's talk more over email</footer>
    )
}

export default Footer;